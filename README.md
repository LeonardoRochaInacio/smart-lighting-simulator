# mirari-sos-microservices
Mirari Node-JS microservice specific to be deployed in Cloud Run Job

## Docker compose build
```bash  
docker-compose up -d --build
```

## Acessar terminal docker
```bash 
docker exec -it microservice_s1 sh
```
